{{- if .Values.zookeeper.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "zookeeper.fullname" . }}
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    component: "{{ .Values.zookeeper.name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
  {{- if .Values.zookeeper.persistence.storageClass }}
    volume.beta.kubernetes.io/storage-class: {{ .Values.zookeeper.persistence.storageClass | quote }}
  {{- else }}
    volume.alpha.kubernetes.io/storage-class: default
  {{- end }}
spec:
  accessModes:
    - {{ .Values.zookeeper.persistence.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.zookeeper.persistence.size | quote }}
{{- end }}